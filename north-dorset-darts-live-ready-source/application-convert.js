document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="application-convert.css">');
function addConvertButtons(){document.querySelectorAll('#applications-body .status-change').forEach(select=>{const cell=select.parentElement,id=select.dataset.id,item=state.applications.find(application=>String(application.id)===String(id));if(!item||cell.querySelector('[data-convert-application]')||cell.querySelector('.converted-label'))return;if(item.converted_resource){cell.insertAdjacentHTML('beforeend',`<span class="converted-label">Added to ${escapeHtml(item.converted_resource)}</span>`)}else if(item.status!=='Declined'){const button=document.createElement('button');button.type='button';button.className='convert-application';button.dataset.convertApplication=id;button.textContent='Add to league';cell.append(button)}})}
async function convertApplication(id){const data=await api('admin/bootstrap'),item=data.applications.find(application=>String(application.id)===String(id));if(!item)return;let payload={},displayName=item.name;if(item.interest_type==='Player'){const teams=data.teams.map(team=>`${team.id}: ${team.name}`).join('\n'),team=prompt(`Add ${item.name} to which team? Enter a team number, or leave blank if not decided:\n\n${teams}`,'');if(team===null)return;payload.team_id=team}else if(item.interest_type==='Team'){const teamName=prompt('Team name','');if(teamName===null||!teamName.trim())return;const division=prompt('Which division should this team join?','Division Four');if(division===null||!division.trim())return;payload.record_name=displayName=teamName.trim();payload.division=division.trim()}else{const venueName=prompt('Venue name','');if(venueName===null||!venueName.trim())return;payload.record_name=displayName=venueName.trim()}const description=item.interest_type==='Player'?'registered player':item.interest_type==='Team'?'league team':'league venue';if(!confirm(`Create ${displayName} as a ${description}?`))return;await api(`admin/applications/${id}/convert`,{method:'POST',body:JSON.stringify(payload)});await loadData();banner(`${displayName} has been added as a ${description}.`)}
document.addEventListener('click',event=>{const button=event.target.closest('[data-convert-application]');if(button){button.disabled=true;convertApplication(button.dataset.convertApplication).catch(error=>{banner(error.message,'error');button.disabled=false})}});new MutationObserver(addConvertButtons).observe(document.querySelector('#applications-body'),{childList:true,subtree:true});addConvertButtons();
