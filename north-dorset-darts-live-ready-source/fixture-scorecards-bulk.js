const bulkSafe=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));const bulkMain=document.querySelector('#bulk-scorecards'),divisionSelect=document.querySelector('#bulk-division'),scopeSelect=document.querySelector('#bulk-scope');let bulkData;
const fixtureDate=value=>new Date(value).toLocaleString('en-GB',{weekday:'long',day:'numeric',month:'long',year:'numeric',hour:'2-digit',minute:'2-digit'});const gameRows='<tr><td>Doubles 1 · BO3</td><td></td><td></td><td></td><td></td></tr><tr><td>Doubles 2 · BO3</td><td></td><td></td><td></td><td></td></tr><tr><td>Singles 1 · BO5</td><td></td><td></td><td></td><td></td></tr><tr><td>Singles 2 · BO5</td><td></td><td></td><td></td><td></td></tr><tr><td>Singles 3 · BO5</td><td></td><td></td><td></td><td></td></tr><tr><td>Singles 4 · BO5</td><td></td><td></td><td></td><td></td><td></td></tr>';
function roster(teamId){const players=bulkData.players.filter(player=>String(player.team_id)===String(teamId)&&player.registration_status==='registered');return players.length?players.map(player=>`<span>□ ${bulkSafe(player.name)}</span>`).join(''):'<span>No registered players</span>'}
function achievementRows(fixture){const players=bulkData.players.filter(player=>[String(fixture.home_team_id),String(fixture.away_team_id)].includes(String(player.team_id))&&player.registration_status==='registered');const teamName=id=>String(id)===String(fixture.home_team_id)?fixture.home_team:fixture.away_team;return players.map(player=>`<tr><td>${bulkSafe(player.name)}</td><td>${bulkSafe(teamName(player.team_id))}</td><td></td><td></td><td></td><td></td></tr>`).join('')||'<tr><td colspan="6">No registered players</td></tr>'}
function card(fixture,index,total){return `<article class="document result-sheet bulk-card"><span class="sheet-number">Scorecard ${index+1} of ${total}</span><header class="document-header"><img src="crest.png" alt="League crest"><div><span class="eyebrow"><b>${bulkSafe(bulkData.settings?.current_season||'Current season')}</b> official scorecard</span><h1>Match Result Sheet</h1><div class="meta">${bulkSafe(fixture.competition||'League')}${fixture.round_name?` · ${bulkSafe(fixture.round_name)}`:''} · 501/1001</div></div></header><div class="details-grid"><div><div class="field">${fixtureDate(fixture.starts_at)}</div><span class="small">Fixture date</span></div><div><div class="field">${bulkSafe(fixture.venue_name||'To be confirmed')}</div><span class="small">Venue</span></div><div><div class="field">${bulkSafe(fixture.home_team)}</div><span class="small">Home team</span></div><div><div class="field">${bulkSafe(fixture.away_team)}</div><span class="small">Away team</span></div></div><div class="bulk-rosters"><section><h2>Home registered players</h2><div class="bulk-roster">${roster(fixture.home_team_id)}</div></section><section><h2>Away registered players</h2><div class="bulk-roster">${roster(fixture.away_team_id)}</div></section></div><h2>Six 501 games</h2><table class="score-table"><thead><tr><th>Game</th><th>Home player(s)</th><th>Legs</th><th>Legs</th><th>Away player(s)</th></tr></thead><tbody>${gameRows}</tbody></table><h2>Beer leg — one leg of 1001</h2><table class="score-table"><tbody><tr><td>${bulkSafe(fixture.home_team)}</td><td>Winner:</td><td>${bulkSafe(fixture.away_team)}</td></tr></tbody></table><div class="page-break"></div><h2>Individual achievements</h2><table class="score-table achievement-table"><thead><tr><th>Player</th><th>Team</th><th>Singles win</th><th>180s</th><th>Highest checkout</th><th>Highest shot-in</th></tr></thead><tbody>${achievementRows(fixture)}</tbody></table><h2>Captain verification</h2><div class="signature-grid"><div><div class="field"></div><span class="small">Home captain signature</span></div><div><div class="field"></div><span class="small">Away captain signature</span></div></div></article>`}
function renderBulk(){let fixtures=bulkData.fixtures.filter(item=>item.status==='scheduled'&&new Date(item.starts_at)>=new Date()&&(divisionSelect.value==='all'||bulkData.teams.find(team=>String(team.id)===String(item.home_team_id))?.division===divisionSelect.value)).sort((a,b)=>String(a.starts_at).localeCompare(String(b.starts_at)));if(scopeSelect.value==='next'){const seen=new Set;fixtures=fixtures.filter(item=>{if(seen.has(item.home_team_id))return false;seen.add(item.home_team_id);return true})}bulkMain.innerHTML=fixtures.length?fixtures.map((fixture,index)=>card(fixture,index,fixtures.length)).join(''):'<p class="bulk-empty">No upcoming home fixtures match this selection.</p>'}
async function startBulk(){const response=await fetch('/api/admin/bootstrap',{credentials:'same-origin'}),data=await response.json();if(!response.ok)throw new Error(data.error||'Unable to load fixtures');bulkData=data;const divisions=[...new Set(data.teams.map(team=>team.division).filter(Boolean))];divisionSelect.insertAdjacentHTML('beforeend',divisions.map(name=>`<option>${bulkSafe(name)}</option>`).join(''));renderBulk()}divisionSelect.addEventListener('change',renderBulk);scopeSelect.addEventListener('change',renderBulk);document.querySelector('#bulk-print').addEventListener('click',()=>window.print());startBulk().catch(error=>bulkMain.innerHTML=`<p class="bulk-empty">${bulkSafe(error.message)}</p>`);
