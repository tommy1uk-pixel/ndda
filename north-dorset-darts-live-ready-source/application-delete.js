function addApplicationDeleteButtons(){if(state.user?.role!=='administrator')return;document.querySelectorAll('#applications-body .status-change').forEach(select=>{const cell=select.closest('td');if(cell.querySelector('[data-delete-application]'))return;const button=document.createElement('button');button.type='button';button.className='delete-application';button.dataset.deleteApplication=select.dataset.id;button.textContent='Delete';cell.append(button)})}
const renderApplicationsBeforeDelete=renderApplications;renderApplications=function(){renderApplicationsBeforeDelete();addApplicationDeleteButtons()};
document.head.insertAdjacentHTML('beforeend','<style>.delete-application{margin-left:6px;color:#9c2525;border-color:#d9b4b4;background:#fff}.delete-application:hover{background:#9c2525;color:#fff}</style>');
document.addEventListener('click',async event=>{const button=event.target.closest('[data-delete-application]');if(!button)return;const application=state.applications.find(item=>String(item.id)===button.dataset.deleteApplication);if(!application)return;const converted=application.converted_resource?`\n\nThe ${application.converted_resource.replace(/s$/,'')} already created from it will NOT be removed.`:'';if(!confirm(`Permanently delete the application from ${application.name}? Its submitted player details will also be deleted.${converted}`))return;button.disabled=true;try{await api(`admin/applications/${application.id}`,{method:'DELETE'});await loadData();banner('Application and its submitted player details were permanently deleted.')}catch(error){banner(error.message,'error');button.disabled=false}});
addApplicationDeleteButtons();
