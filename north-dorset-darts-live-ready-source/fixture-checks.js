document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="fixture-checks.css">');
const fixturesView=document.querySelector('#fixtures-view'),fixtureCards=document.querySelector('#fixtures-list');
if(fixturesView&&fixtureCards){fixtureCards.insertAdjacentHTML('beforebegin','<div class="panel fixture-check-panel"><div class="fixture-check-head"><div><h2>Fixture clash check</h2><p>Check teams, venues and protected league dates before publishing the schedule.</p></div><button id="check-fixtures">Check schedule</button></div><div class="fixture-check-result" id="fixture-check-result"><span>Run the check after adding or generating fixtures.</span></div></div>')}
const safeCheck=value=>String(value??'').replace(/[&<>"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[char])),checkDate=value=>new Date(value).toLocaleString('en-GB',{weekday:'short',day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'});
document.querySelector('#check-fixtures')?.addEventListener('click',async event=>{const button=event.currentTarget,result=document.querySelector('#fixture-check-result');button.disabled=true;button.textContent='Checking…';try{const response=await fetch('/api/admin/fixture-clashes',{credentials:'same-origin'}),data=await response.json();if(!response.ok)throw new Error(data.error||'Unable to check fixtures');if(!data.total){result.innerHTML='<div class="fixture-check-ok"><strong>No clashes found.</strong> The current schedule has no duplicate teams, venue collisions or protected-date conflicts.</div>';return}const items=[...data.team_clashes.map(item=>({type:'Team booked twice',title:`${item.home_team} v ${item.away_team}`,detail:`Also clashes with ${item.other_home} v ${item.other_away} · ${checkDate(item.starts_at)}`})),...data.venue_clashes.map(item=>({type:'Venue collision',title:item.venue_name,detail:`${item.home_team} v ${item.away_team} and ${item.other_home} v ${item.other_away} · ${checkDate(item.starts_at)}`})),...data.event_clashes.map(item=>({type:'Protected date',title:item.event_title,detail:`${item.home_team} v ${item.away_team} is scheduled on the same date · ${checkDate(item.starts_at)}`}))];result.innerHTML=`<div class="fixture-check-warning"><strong>${data.total} schedule ${data.total===1?'issue':'issues'} found.</strong> Edit the affected fixtures or event dates, then check again.</div><div class="fixture-clash-list">${items.map(item=>`<article class="fixture-clash"><span>${safeCheck(item.type)}</span><div><strong>${safeCheck(item.title)}</strong><small>${safeCheck(item.detail)}</small></div></article>`).join('')}</div>`}catch(error){result.innerHTML=`<div class="fixture-check-warning">${safeCheck(error.message)}</div>`}finally{button.disabled=false;button.textContent='Check schedule'}});
