document.querySelectorAll('[data-export]').forEach(button=>button.addEventListener('click',async()=>{const original=button.textContent;button.disabled=true;button.textContent='Preparing…';try{const response=await fetch(`/api/admin/export?type=${encodeURIComponent(button.dataset.export)}`,{credentials:'same-origin'});if(!response.ok){const error=await response.json().catch(()=>({}));throw new Error(error.error||'Unable to create download')}const blob=await response.blob(),link=document.createElement('a'),disposition=response.headers.get('content-disposition')||'',match=disposition.match(/filename="([^"]+)"/);link.href=URL.createObjectURL(blob);link.download=match?.[1]||`north-dorset-darts-${button.dataset.export}`;document.body.appendChild(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(link.href),1000)}catch(error){alert(error.message)}finally{button.disabled=false;button.textContent=original}}));
const exportCards=document.querySelector('#exports-view .cards-list');if(exportCards){exportCards.insertAdjacentHTML('afterbegin','<article class="record-card"><div><span class="tag">Print centre</span><h3>Committee reports</h3><p>Print current division tables, fixture lists and published results directly from live league data.</p></div><a href="print-centre.html" target="_blank">Open print centre</a></article>')}
if(exportCards){exportCards.insertAdjacentHTML('afterbegin','<article class="record-card"><div><span class="tag gold">Presentation night</span><h3>Season awards</h3><p>Automatically calculate league champions, cup winners and individual performance awards.</p></div><a href="season-awards.html" target="_blank">Open awards report</a></article>')}
if(exportCards){exportCards.insertAdjacentHTML('afterbegin','<article class="record-card"><div><span class="tag gold">League management</span><h3>Competitions, sponsors & recovery</h3><p>Run singles and doubles draws, manage public sponsors and create guarded recovery points.</p></div><a href="league-management.html">Open management centre</a></article>')}
